import React from 'react'

// This page layout component is shared with in all pages within the posts directory. It is used to add a common layout to all pages within the posts directory. Create a new file at app/posts/layout.js and add the following code:

const PageLayout = () => {
  return (
    <div>
      Navigate to top
    </div>
  )
}

export default PageLayout
